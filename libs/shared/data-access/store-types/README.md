# shared-data-access-store-types

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-data-access-store-types` to build the library.

## Running unit tests

Run `nx test shared-data-access-store-types` to execute the unit tests via [Jest](https://jestjs.io).
