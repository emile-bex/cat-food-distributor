# shared-distributors-data-access

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-distributors-data-access` to build the library.

## Running unit tests

Run `nx test shared-distributors-data-access` to execute the unit tests via [Jest](https://jestjs.io).
