# shared-util-asserts

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-util-asserts` to build the library.

## Running unit tests

Run `nx test shared-util-asserts` to execute the unit tests via [Jest](https://jestjs.io).
